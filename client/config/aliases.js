/* global module */
/**
 * Aliases for the application
 */

function buildConfig(join) {
  // Add references to the project. For example, you can import fetch using
  // 'fetch' instead of 'shared/utils/fetch'
  return {

  }
}

// Export the funciton to build the config.
module.exports = buildConfig;
